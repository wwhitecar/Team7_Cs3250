spring:
  profiles:
    active: default

sql:
  insertUserSql: insert into users (student_id, first_name, last_name) values (:student_id,:first_name,:last_name)
  getUserByIdSql: select * from users where student_id = :student_id
  deleteUserByIdSql: delete from users where student_id = :student_id
  updateUserByIdSql: update users set first_name = :first_name, last_name = :last_name where student_id = :student_id

sqlcourse:
  insertCourseSql: insert into courses (department, course_number, credits, description, learning_objective, prereqs, coreqs) values (:description, :course_number, :credits, :description, :learning_objective, :prereqs, :coreqs)
  getCourseByNumber: select * from courses where course_number =:courseNumber
  deleteCourseByNumber: delete from courses where course_number
  updateCourseByNumber : update courses set department = :department, credits = :credits, description =:description, learning_objectives = :learning_objectives, prereqs = :prereqs, coreqs = :coreqs

schedule:
  database:
    url: jdbc:mysql://localhost:3306/schedule
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
    test-on-borrow: true
    validation-query: SELECT 1

---
spring:
  profiles: mac

schedule:
  database:
    url: jdbc:mysql://localhost:3306/schedule?autoReconnect=true&useSSL=false&useJDBCCompliantTimezoneShift=true&useLegacyDatetimeCode=false&serverTimezone=UTC
    username: root
    password: otis.missouri
    driver-class-name: com.mysql.cj.jdbc.Driver
    test-on-borrow: true
    validation-query: SELECT 1